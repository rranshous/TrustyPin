pragma solidity >=0.4.21 <0.7.0;

import "truffle/Assert.sol";
import "truffle/DeployedAddresses.sol";
import "../contracts/TrustyPin.sol";

contract TestTrustyPin_AddPin {
  // function othertestAddsToLookup() public {
  //   string memory ipfsHash = "fakeHash";
  //   uint chunksToAllocate = 32;
  //   TrustyPin trustyPin = TrustyPin(DeployedAddresses.TrustyPin());
  //   trustyPin.addPin(ipfsHash, chunksToAllocate);
  //   Assert.equal(trustyPin.getPinAllocation(ipfsHash),
  //                chunksToAllocate);
  // }
}
